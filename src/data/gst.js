/**
 * Frontend GST helpers — re-export shared domain.
 */
export {
  GST_RATE,
  GST_RATE_PERCENT,
  GST_BUSINESS_STATE,
  applyExclusiveGst,
  splitInclusiveGst,
  allocateGst,
  buildGstBreakdown,
} from '@pahadlink/shared/gst'
